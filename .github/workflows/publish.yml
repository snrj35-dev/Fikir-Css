name: publish-npm

on:
  push:
    tags:
      - "v*"

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          registry-url: "https://registry.npmjs.org"

      - name: Install dependencies
        run: npm ci

      - name: Build CSS bundle
        run: npm run build

      - name: Run CI test suite
        run: npm run test:ci

      - name: Determine release channel
        id: release
        run: |
          VERSION=$(node -p "JSON.parse(require('node:fs').readFileSync('package.json', 'utf8')).version")
          if [[ "$VERSION" == *"-beta."* ]]; then
            echo "channel=beta" >> "$GITHUB_OUTPUT"
          elif [[ "$VERSION" == *"-rc."* ]]; then
            echo "channel=rc" >> "$GITHUB_OUTPUT"
          else
            echo "channel=latest" >> "$GITHUB_OUTPUT"
          fi
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Check if version already published
        id: check
        run: |
          VERSION=${{ steps.release.outputs.version }}
          PUBLISHED=$(npm view "fikir-css@$VERSION" version 2>/dev/null || echo "")
          if [ "$PUBLISHED" = "$VERSION" ]; then
            echo "already_published=true" >> "$GITHUB_OUTPUT"
            echo "ℹ️ fikir-css@$VERSION is already on npm — skipping publish."
          else
            echo "already_published=false" >> "$GITHUB_OUTPUT"
            echo "✅ fikir-css@$VERSION not yet published — will publish."
          fi

      - name: Verify publish (dry run)
        if: steps.check.outputs.already_published == 'false'
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
          RELEASE_CHANNEL: ${{ steps.release.outputs.channel }}
        run: |
          if [ "$RELEASE_CHANNEL" = "latest" ]; then
            npm publish --dry-run --access public
          else
            npm publish --dry-run --access public --tag "$RELEASE_CHANNEL"
          fi

      - name: Publish to npm
        if: steps.check.outputs.already_published == 'false'
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
          RELEASE_CHANNEL: ${{ steps.release.outputs.channel }}
        run: |
          if [ "$RELEASE_CHANNEL" = "latest" ]; then
            npm publish --provenance --access public
          else
            npm publish --provenance --access public --tag "$RELEASE_CHANNEL"
          fi
