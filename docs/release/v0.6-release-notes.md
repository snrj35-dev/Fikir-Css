# v0.6.0 Release Notes

> Status: beta — `0.6.0-beta.1` published on npm under `@beta` dist-tag.
> Install: `npm install fikir-css@beta`

## Focus: Stability & Distribution (M9) + Theme System (M10)

### M9 — Stability & Distribution

#### Changed
- `package.json` `repository`, `homepage`, `bugs` URLs updated to `snrj35-dev`
- README: npm/CDN install options marked as **pending** with warning; CDN example replaced with GitHub Pages URL
- `site/index.html`: removed broken CDN fallback (`onerror` → jsDelivr) — package not yet on npm
- `playground/index.html`: version badge corrected `v1.0-M2` → `v0.5.0`
- `publish.yml`: added `--dry-run` verification step before actual publish; added `--tag beta` for prerelease dist-tag
- `CHANGELOG.md`: v0.6.0-beta Unreleased section added

#### Fixed
- CI failures: `docs-link-audit` (missing stub paths) and `tasklist-hardening` (carry-over items) — 245/245 tests green

#### Added
- `docs/guides/theme-system.md` — stub, full content in M10
- `examples/vue-vite/README.md` — stub, full example in M11
- `examples/svelte-vite/README.md` — stub, full example in M11

### M10 — Theme System _(in progress)_

- Gallery theme switcher dropdown — in progress
- `docs/guides/theme-system.md` full content — in progress
- Token explorer two-theme comparison — planned

## Breaking Changes

None.

## Migration from v0.5.0

No API changes. Update the stylesheet reference if using the GitHub Pages CDN:

```html
<!-- Before -->
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/fikir-css@0.5.0/dist/fikir.css" />

<!-- After (beta) -->
<link rel="stylesheet" href="https://snrj35-dev.github.io/Fikir-Css/dist/fikir.css" />
```
