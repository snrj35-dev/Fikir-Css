{
  "name": "fikir-css-mvp",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-css.mjs",
    "build:css": "node scripts/build-css.mjs",
    "test:source": "node --test tests/source/*.test.mjs",
    "test:build": "node --test tests/build/*.test.mjs",
    "test:ci": "npm run test:source && npm run build && npm run test:build",
    "test": "npm run test:ci"
  }
}
