# Fikir CSS × Vue 3 Example

> Status: planned — full example coming in M11.

A working Vue 3 + Vite example demonstrating Fikir CSS integration.

## Planned content
- `useTheme()` composable for dark/compact mode
- Typed resolvers (`resolveBtn`, `resolveAlert`) in templates
- Modal component with `createFocusTrap` helper
- `<script setup>` + TypeScript
